#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
	char a[50];
	char b[50];
	 printf("saisir le premiere chaine : ");
	 scanf ("%s",&a);
    printf("saisir le premiere chaine : ");
	 scanf ("%s",&b);
	 if(strcmp(a,b)==0)
	 printf("les chaines sont egale ");
	else 
	printf(" les chaines sont defferent ");
 
	
	
	
	
		
	return 0;
}
